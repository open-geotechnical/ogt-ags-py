image: ubuntu

pages:
  script:
  - apt-get update -qq
  - apt-get install -y -qq python-pip python-dev py-pillow graphviz
  - pip install -r requirements.txt
  - sh build_docs.sh
  artifacts:
    paths:
    - public
  only:
  - master
